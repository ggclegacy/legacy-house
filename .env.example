# Required by database commands and readiness checks. Use a PostgreSQL connection string.
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE

# Optional at runtime. Defaults to 3000 and is supplied automatically by Replit.
PORT=3000

